<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>

        <!--
          ⚠️ `windowOptOutEdgeToEdgeEnforcement` 를 **뺐다** (2026-08-06, 대상 API 36 전환).
             이 플래그는 **API 36 에서는 무시된다** — 남겨두면 "꺼져 있다"고 착각하게 된다.
             대신 RN 0.81 의 `edgeToEdgeEnabled=true`(gradle.properties)로 인셋을 처리한다.
             화면은 react-native-safe-area-context 의 SafeAreaProvider 를 이미 쓰고 있다.
        -->
    </style>

</resources>
