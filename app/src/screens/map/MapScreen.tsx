/** 루트 위치 탭 (v1 idx2 MapScreen(mountain:_mountain)). Phase 2-3에서 구현(지도 SDK [TBD]). */
import React from 'react';
import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

export const MapScreen: React.FC = () => (
  <PlaceholderScreen title="루트 위치" note="v1 탭2 MapScreen (map_screen.dart, 711줄)" />
);
